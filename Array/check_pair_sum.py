# Given an array and a number x, check for pair in array with sum as x.

def check_pair_sum(arr, x):
    arr.sort()
    
if __name__ == '__main__':
    arr = [1, 4, 45, 6, 10, -8]
    x = 16
    check_pair_sum(arr, x)
